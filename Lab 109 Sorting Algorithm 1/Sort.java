
/**
 * From our psuedocode, designed to oragnize array into new array, smallest to largest
 *
 * @author (Ziggy Sheynin)
 * @version (aLab 109)
 * assume all numbers are positive
 */
import java.util.*;
public class Sort
{
    public static void main(){

        //declare and initialze array
        int [] unordered = new int [10];
        for (int i = 0; i < unordered.length; i++){
            int x = (int)(Math.random()*10)+1;
            int [i] = new int(x);
        }

    }
    //declare and initialize varible small, set = 0
    //traverse array

    //find all values = to small
    //put values into new array
    //if no value = small, add one to small and repeat

}
